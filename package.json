{
  "name": "react-and-rails",
  "engines": {
    "node": "11.4.0",
    "yarn": "1.10.1"
  },
  "scripts": {
    "build": "yarn --cwd app/client install && yarn --cwd app/client build",
    "deploy": "cp -a app/client/build/. public/",
    "heroku-postbuild": "yarn build && yarn deploy && echo 'Client built!'"
  }
}